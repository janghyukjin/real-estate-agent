[project]
name = "real-estate-agent"
version = "0.1.0"
description = "한국 부동산 내집마련 AI 에이전트"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "rich>=13.0",
    "pandas>=2.0",
    "anthropic>=0.40",
    "PublicDataReader>=2024.0",
]
